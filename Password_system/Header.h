#ifndef password_sys
#define password_sys
#include <iostream>
#include <regex>
#include <string>
using namespace std;
void constraints(void);
void is_user(void);
extern string name;
extern string password;
extern string old_password;
#endif